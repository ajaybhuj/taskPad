<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheet/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form p-10 text-white">
        <form action="/create" method="post">
          <input
            class="block px-5 py-2 bg-zinc-800 w-full rounded-md outline-none"
            type="text"
            placeholder="Title"
            name="title"
            required
          />
          <textarea
            class="block px-5 mt-2 w-full rounded-md outline-none resize-none py-2 bg-zinc-800"
            placeholder="Write your task here"
            name="details"
            required
          ></textarea>
          <input
            class="mt-2 bg-blue-600 px-5 py-2 rounded"
            type="submit"
            value="Create Task"
          />
        </form>
      </div>
      <div class="tasks p-10 flex flex-wrap gap-3">
        <% if (files && files.length > 0) { %> <% files.forEach(function(file) {
        %>
        <div class="task min-w-72 px-3 py-4 rounded bg-zinc-800">
          <h1 class="text-white text-2xl tracking-tighter"><%= file %></h1>
          <a class="text-blue-500 inline-block mt-3" href="/files/<%= file %>"
            >Read More</a
          >
        </div>
        <% }) %> <% } else { %>
        <h3 class="text-zinc-600">No tasks yet</h3>
        <% } %>
      </div>
    </div>
    <script src="/javascript/script.js"></script>
  </body>
</html>
